on:
  push:
    branches:
      - 'main'

jobs:
  job-0:
    runs-on: ubuntu-22.04
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - run: |
          set -eux
          echo ATTACK_SURFACE="$(echo "123\n123\n123\n" | jq -Rs '.')" >> $GITHUB_ENV

      - run:
          echo ${{ env.ATTACK_SURFACE }}

      - run:
          echo ${{ toJSON(env.ATTACK_SURFACE) }}

      - run: |
          set -eux
          echo ${{ env.ATTACK_SURFACE }}
