name: Test

on:
  workflow_run:
    workflows: [Build]
    types: [completed]

jobs:
  job-0:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello from workflow_run ${{ github.event.workflow_run.head_sha }}
